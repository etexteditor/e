#ifndef __BUNDLEITEMTYPE_H__
#define __BUNDLEITEMTYPE_H__

enum BundleItemType {
	BUNDLE_NONE,
	BUNDLE_BUNDLE,
	BUNDLE_COMMAND,
	BUNDLE_SNIPPET,
	BUNDLE_DRAGCMD,
	BUNDLE_PREF,
	BUNDLE_LANGUAGE,

	BUNDLE_MENU,
	BUNDLE_SUBDIR,
	BUNDLE_SEPARATOR
};

#endif
